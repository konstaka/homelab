apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: argocd
  namespace: argocd
spec:
  project: default
  source:
    repoURL: http://dock.internal/konstaka/homelab.git
    path: argocd
    targetRevision: HEAD
    helm:
      valueFiles:
        - "values.yaml"
        - "environments/values-{{ .Values.cluster }}.yaml"
  destination:
    namespace: argocd
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
