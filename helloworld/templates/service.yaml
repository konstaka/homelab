apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.name }}"
  namespace: "{{ .Values.namespace }}"
spec:
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: "{{ .Values.name }}"
