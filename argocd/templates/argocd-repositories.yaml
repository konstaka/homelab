apiVersion: v1
kind: Secret
metadata:
  name: gitops-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  type: git
  url: "{{ .Values.gitBaseURL }}/{{ .Values.gitNamespace }}/homelab.git"
  project: default
